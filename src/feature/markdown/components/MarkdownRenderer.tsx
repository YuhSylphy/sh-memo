type Props = {
	documentId: string;
};

export function MarkdownRenderer({ documentId }: Props) {
	// TODO: fetch and render markdown document by documentId
	return <div>MarkdownRenderer: {documentId}</div>;
}
